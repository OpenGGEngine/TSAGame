package com.opengg.components;

public class InteractableAI extends WorldAI {
    String dialogue = "bird";
    boolean allowInteraction = true;

    @Override
    public void update(float delta){
        super.update(delta);
    }
}
